import React from "react";

import Typography from "@mui/material/Typography";

const App = () => {
  return <div>
    <Typography variant="h3">
      React Auth
    </Typography>
  </div>;
};

export default App;
